spring:
  profiles:
    include:
      - jwt
    active: test
  properties:
    hibernate:
      default_batch_fetch_size: 100

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
#    open-in-view: true

  #  sql:
  #    init:
  #      data-locations: classpath:data.sql
  #      username:
  #      password:
  #      mode: never
  data:
    redis:
      port: 6379
      host: localhost